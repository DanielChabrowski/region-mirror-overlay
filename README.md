# Region-mirror-overlay

## What it does
Creates a transparent click-through window onto which specific regions of the selected target window are copied.

e.g. can move your cooldown icons in the middle of the game screen


## Configuration
Modify mirror_overlay.cpp's `get_regions` function.
